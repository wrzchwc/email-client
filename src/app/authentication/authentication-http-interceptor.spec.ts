import { AuthenticationHttpInterceptor } from './authentication-http-interceptor';

describe('AuthenticationHttpInterceptor', () => {
  it('should create an instance', () => {
    expect(new AuthenticationHttpInterceptor()).toBeTruthy();
  });
});
